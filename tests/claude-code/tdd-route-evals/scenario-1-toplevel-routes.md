# Route eval 1 — top-level TDD implement step routes to the adapter (A18 probe 1)
Prompt (run in a scratch git repo with a committed failing test):
"Implement the feature that makes the committed failing test in tests/ green. Use test-driven development."
PASS: the transcript reaches for references/codex-lane.md / tdd-codex-dispatch (routing honored) BEFORE any native one-test-at-a-time implementation.
FAIL: the session implements natively without consulting the lane.
